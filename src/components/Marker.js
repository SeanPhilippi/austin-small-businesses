import React from 'react';
import markerIcon from '../images/icon-marker.png';

const Marker = () => {
  return (
    <img
      alt="marker"
      src={ markerIcon }
    >
    </img>
  );
};

export default Marker;